# Prompt Wars 1.2.0 TestFlight release

The user requested “bump the version, deploy and submit” after the center Battle action was restored, then explicitly approved “yes, to test flight.” This record supersedes the mobile preparation status in the [earlier deployment log](2026-09-14-ux-game-integrity.md).

## Prepared and verified

- Version changed from 1.1.0 to **1.2.0** in `package.json`, `app.config.js`, the iOS `Info.plist`, and both Xcode marketing-version settings. EAS remote auto-increment allocated **iOS build 9**.
- Release includes the restored raised center Battle action, the approved UX/game-integrity implementation, and the customization accessibility follow-ups. Existing audio changes remain byte-identical to the starting baseline.
- Fresh verification passed **130 Jest suites / 1,069 tests**, app TypeScript, and standard lint with zero errors and the existing guarded Reanimated require warning. Both native project/property-list files passed `plutil -lint`.
- Linked Supabase migration dry run reported **“Remote database is up to date.”** The backend was already deployed in the earlier attempt; this client/version change introduced no backend migration or function change. New-rule matchmaking and appeals remain gated off pending their recorded acceptance checks.
- EAS local-only archive inspection produced **319 files / 18,458,896 bytes**, including the center Battle change and version bump. No Git history, environment files, credentials, backend, documentation, tests or agent records are in the archive. The checked secret patterns had no matches. [Archive verification](../audits/2026-09-14-release-1.2.0/archive-check.json). Source-tree SHA256: `15977506f99eaec674e3f92535983030583492823fb0d847df7401bf0373f0c8`.

## Delivery status

The source upload succeeded after the explicit TestFlight confirmation. The signed iOS production build for **version 1.2.0, build 9** finished successfully. Its automatic TestFlight submission is queued for upload. At **16:12 UTC on 14 September**, EAS reported `IN_QUEUE`, normal priority, no error and no submission logs; the submission references the correct completed build. Apple upload and TestFlight availability are not yet confirmed. The local status watcher has stopped; the cloud submission remains queued and will proceed independently.

- [EAS build](https://expo.dev/accounts/prompt-wars/projects/prompt-wars/builds/ac588207-838f-4813-96fc-fb3db760fb36): `ac588207-838f-4813-96fc-fb3db760fb36`.
- [TestFlight submission](https://expo.dev/accounts/prompt-wars/projects/prompt-wars/submissions/882783fd-0ca0-46c6-8f67-6db8bb2ea45e): `882783fd-0ca0-46c6-8f67-6db8bb2ea45e`.

The command used the existing production EAS environment, existing distribution certificate/provisioning profile and existing App Store Connect API key. No new signing credentials or permissions were created. [Signed IPA](https://expo.dev/artifacts/eas/xYBn8fk5KHPAr9wzvddEtbYfhdYSJADiV81BDU9vO3Y.ipa). Final submission outcome is pending.

The destination is the existing Expo project **`@prompt-wars/prompt-wars`**, project ID `d4c525e1-0a95-4c6a-b1ee-106fa2f03f46`. iOS submission targets existing App Store Connect app `6788787677`, bundle `gg.promptwars.app`, through TestFlight. The latest confirmation is scoped to TestFlight; no Android release was dispatched in this continuation.

Earlier automatic approval review rejected the source upload because it required explicit authorization naming the payload and destination. A read-only local-signing check found zero valid identities and no Fastlane installation. No alternative upload was attempted; the explicit user confirmation resolved the block before the EAS command was executed.

## Other release gates

Google Play submission additionally needs the service-account JSON file path requested earlier, or an existing authorized Play Console session. The earlier check found no configured EAS Google service-account key; the available browser account opened developer-account registration. No account was created and no key contents should be entered in chat or source control.

Public store release and combat/appeal activation still follow the [acceptance matrix](../UX_GAME_INTEGRITY_ACCEPTANCE.md). No historic battle rejudging, old exit-fee refunds, schema rollback, source commit or Git push was performed.
